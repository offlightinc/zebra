# Optional developer override for Markdown chat pill Codex sessions.
# When set, Codex uses this repo as its workspace and adds the markdown file's
# folder with --add-dir so vault/plan notes remain available as context.
CMUX_MARKDOWN_CHAT_CODEX_TARGET_REPO=/Users/han/zebra
